package marvin.ms.common;

/**
 * A function is defined without a shift an the Y axis. Or otherwise the
 * framework may produce wrong results without careful implementation.
 * 
 * @author pizzadox9999
 */

public interface Function {
    public float f(float x);
}
